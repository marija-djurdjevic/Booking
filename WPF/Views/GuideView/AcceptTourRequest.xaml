<Page x:Class="BookingApp.WPF.Views.GuideView.AcceptTourRequest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      xmlns:local="clr-namespace:BookingApp.WPF.Views.GuideView"
      mc:Ignorable="d" 
      
      Title="AcceptTourRequest" Height="768" Width="1024">

    <Grid>
        <!-- Navigaciona traka -->
        <Grid VerticalAlignment="Top" Height="71" Background="LightGray">
            <!-- Slika u prvoj koloni -->
            <Button VerticalAlignment="Top"
                HorizontalAlignment="Left"
                Command="{Binding SideManuCommand}"
                Background="Transparent"
                BorderThickness="0">
                <Image Source="/Resources/Images/menu5.png"
                    Stretch="Uniform"
                    Width="71"
                    Height="71" />
            </Button>
            <TextBlock VerticalAlignment="Center" FontSize="32" Text="TOURISTS NUMBER" Margin="72,0,86,0"/>
        </Grid>
        <!-- Grid za sadržaj -->
        <Grid Background="White" Margin="0,76,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="320.387"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!-- Tekst -->

            <TextBlock FontSize="32" Margin="24,60,680,522" Grid.ColumnSpan="2">
                    <Run Text="Date ranges provided: "/>
            </TextBlock>



            <TextBlock FontSize="32" Margin="24,338,680,239" Grid.ColumnSpan="2">
         <Run Text="Start Date and Time: "/>
            </TextBlock>

        </Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,12,0,-12">
            <ListBox
                x:Name="ListBoxDates"
                Margin="367,122,322,451"
                ItemsSource="{Binding FreeDates}">
            </ListBox>
        </ScrollViewer>


        <xctk:DateTimePicker
            x:Name="StartDateTimePicker"
            Grid.Row="0"
            Margin="364,405,319,291"
            Value="{Binding SelectedDateTime, Mode=TwoWay}" />



        <Button Margin="364,564,319,137" Content="Confirm" FontSize="32" Command="{Binding AcceptTourCommand}"></Button>



    </Grid>
</Page>

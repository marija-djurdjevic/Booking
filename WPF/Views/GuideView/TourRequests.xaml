<Page x:Class="BookingApp.WPF.Views.GuideView.TourRequests"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      xmlns:local="clr-namespace:BookingApp.WPF.Converters"
      mc:Ignorable="d" 
      Title="TourRequests" Height="768" Width="1024">
    <Page.Resources>
        <local:ImagePathConverter x:Key="ImagePathConverter" />
    </Page.Resources>

    <Grid Background="White">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="71"/>
                <RowDefinition Height="220"/>
                <RowDefinition Height="400"/>
            </Grid.RowDefinitions>

            <!-- Gornja navigaciona traka -->
            <Grid Grid.Row="0" Height="71" VerticalAlignment="Top" Background="LightGray">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="71"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- Slika u prvoj koloni -->
                <Button Grid.Column="0"
                Command="{Binding SideManuCommand}"
                Background="Transparent"
                BorderThickness="0">
                    <Image Source="/Resources/Images/menu5.png"
                 Stretch="Uniform"
                 Width="71"
                 Height="71" />
                </Button>
                <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="32" Text="START TOUR"/>
            </Grid>




            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="70"/>
                    <RowDefinition Height="70"/>
                    <RowDefinition Height="70"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="516"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Choose location:" FontSize="24" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="16,0,0,0"/>
                <ComboBox  Grid.Column="0" Margin="251,16,105,12" ItemsSource="{Binding Locations}" SelectedItem="{Binding Location}"  />

                <!-- Tourists number -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Tourists number:" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="16,0,0,0"/>
                <xctk:IntegerUpDown
                    Grid.Row="1"
                    Height="42"
                    Increment="1"
                    Minimum="0"
                    RenderTransformOrigin="-0.113,-7.205"
                    Value="{Binding TouristsNumber, Mode=TwoWay}" Margin="251,16,105,12" />

                <!-- Choose language -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Choose language:" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="18,0,0,0"/>
                <ComboBox Grid.Row="2" Grid.Column="0"  ItemsSource="{Binding Languages}" SelectedItem="{Binding Language}" VerticalAlignment="Center" Margin="251,0,105,0" Height="42"/>

                <!-- Start datetime -->
                <TextBlock Grid.Column="1" Text="Start datetime:" FontSize="24" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="28,21,0,0" />
                <xctk:DateTimePicker Grid.Row="0" Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Left"  Value="{Binding StartDateTime, Mode=TwoWay}" Margin="234,16,0,0" Height="42" Width="195" />



                <!-- End datetime -->
                <TextBlock Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Text="End datetime:" FontSize="24" Margin="28,0,0,0" />
                <xctk:DateTimePicker Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left"  Value="{Binding EndDateTime, Mode=TwoWay}" Margin="234,0,0,0" Height="42" Width="195" />


                <Button Grid.Row="2" Grid.Column="1" Content="Search" HorizontalAlignment="Left" VerticalAlignment="Center" Command="{Binding SearchCommand}" Height="50" Width="153" Margin="256,0,0,0" 
                     />



            </Grid>








            <ListBox Grid.Row="2" VerticalAlignment="Top"  ItemsSource="{Binding TourRequests}"  Height="360"
                x:Name="tourListBox"
                 Margin="10"
                SelectedItem="{Binding SelectedTour, Mode=TwoWay}"
                SelectionMode="Single" d:ItemsSource="{d:SampleData ItemCount=5}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="195" Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!--  Slika ture  -->
                            <Image
                                            Grid.Column="0"
                                            Width="150"
                                            Height="230"
                                            Margin="0,7,10,38"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Top"
                                            Stretch="Fill">
                                <Image.Source>
                                    <Binding Converter="{StaticResource ImagePathConverter}" Path="ImagesPaths[0]" />
                                </Image.Source>
                            </Image>

                            <!--  Informacije o turi  -->
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">

                                <TextBlock Margin="0,0,0,20" >
                            <Run Text="Location: " />
                            <TextBlock Margin="140,0,0,0">
                            <Run FontSize="24" Text="{Binding Location.City}" />
                            <Run FontSize="24" Text="," />
                            <Run FontSize="24" Text="{Binding Location.Country}" />
                            </TextBlock>
                        </TextBlock>

                                <TextBlock Margin="0,0,0,20" >
                            <Run Text="Language: " />
                            <TextBlock Margin="125,0,0,0">
                                <Run FontSize="24" Text="{Binding Language}" />
                            </TextBlock>
                        </TextBlock>

                                <TextBlock  >
                            <Run Text="Tourist sign-ups number: " />
                            <Run FontSize="24" Text="{Binding TouristNumber}" />
                                </TextBlock>

                                <Button
                            Width="138"
                            Height="36"
                            Margin="630,0,0,0"
                            Padding="-1"
                            Command="{Binding Path=DataContext.AcceptTourClickCommand, RelativeSource={RelativeSource AncestorType={x:Type Page}}}"
                            CommandParameter="{Binding Id}"
                            Content="ACCEPT"
                            FontSize="28" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Page>
